var should = require('chai').should();
describe('Server Side Specifications', function() {
  describe('Test function', function() {
    it('it should pass a test function', function() {
      true.should.equal(true);
    });
  });
});